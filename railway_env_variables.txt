# Django Core Settings
DJANGO_SETTINGS_MODULE=shortcut.settings_production
SECRET_KEY=)5npnaf#!vmxg)+rqm3^=m-^qb^cr2pmc30nh#zt!8s6vhv-8x
ALLOWED_HOSTS=127.0.0.1,localhost

# MySQL Database Settings
DB_NAME=shortcut_store
DB_USER=shortcut_admin
DB_PASSWORD=ShortcutTest2024!
DB_HOST=your_mysql_host
DB_PORT=3306

# Email (Gmail SMTP)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=shortcut756@gmail.com
EMAIL_HOST_PASSWORD=wijpdbtlxdfseuxq

# CORS and CSRF (for frontend-backend communication)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://localhost:5173

# Admin User (Optional, for auto-creation)
ADMIN_EMAIL=admin@shortcut.com
ADMIN_PASSWORD=admin123ok 
PORT=8000