# Nginx configuration for Shortcut Store

# Serve media files (user uploads, site icons, etc.)
location /media/ {
    alias /home/shortcut/shortcut-store/media/;
    autoindex off;
}

# (Add your existing server and static file config below if needed) 