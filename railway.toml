[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn shortcut.wsgi:application --bind 0.0.0.0:$PORT"
healthcheckPath = "/admin/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

[deploy.envs]
DJANGO_SETTINGS_MODULE = "shortcut.settings_production" 